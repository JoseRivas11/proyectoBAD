/* @model IEnumerable<proyectoBAD.ViewModels.MantenimientosViewModel>

@{
    ViewBag.Title = "Admon. Mantenimientos";

    ViewBag.Pageheader = "Mantenimiento";
}

@section customStyles{
    @Styles.Render("~/bundles/dataTableStyles")
}

<div class="row">
    <div class="col-md-3 col-md-offset-9">
        <a class="btn btn-primary pull-right" href="@Url.Action("Create", "Mantenimientos")"><i class="fa fa-fw fa-plus"></i> Agregar</a>
    </div>
</div>
<br />
<div class="row">
    @if (ViewBag.SuccessMessage != null)
    {
        <div class="col-md-4 col-md-offset-4">
            <div class="alert alert-success alert-dismissable">
                <button class="close" aria-hidden="true" type="button" data-dismiss="alert">×</button>
                @ViewBag.SuccessMessage
            </div>
        </div>
    }
    <div class="col-md-12">
        @if (Model.Count() > 0)
        {
            <table class="table table-striped table-bordered table-hover table-condensed data">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.idMan)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.fechaIniMant)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.fechaFinMant)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.tipoMant)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.equipoFisi)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                {
                       <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.idMan)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.fechaIniMant)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.fechaFinMant)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.tipoMant)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.equipoFisi)
                            </td>

                            <td>
                                <a href="@Url.Action("Edit", "Mantenimientos", new { id = item.idMan })" title="" class="btn btn-primary btn-xs action"
                                   data-original-title="Editar" data-toggle="tooltip" data-placement="top">
                                    <i class="fa fa-fw fa-edit"></i>
                                </a>
                                @*
                                    @Html.ActionLink("Details", "Details", new { id = item.id }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.id })
                                *@
                            </td>
                        </tr>
                    }
                </tbody>

            </table>
        }
        else
        {
            <h3>No hay datos que mostrar</h3>
        }
    </div>
</div>
@section customScripts{
    @Scripts.Render("~/bundles/dataTableScripts")
    <script type="text/javascript">
        $(document).ready(function () {
           $(".data").DataTable({
                responsive: true
            });

            $(".action").tooltip({
                container: "body"
            })
        });
    </script>
}
*/



