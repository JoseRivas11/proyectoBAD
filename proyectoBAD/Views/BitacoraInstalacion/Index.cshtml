
/* @model IEnumerable<proyectoBAD.ViewModels.BitacoraInstalacionViewModel>

@{
    ViewBag.Title = "Admon. Bitacoras de Instalacion";

    ViewBag.Pageheader = "Bitacora Instalacion";
}

@section customStyles{
    @Styles.Render("~/bundles/dataTableStyles")
}

<div class="row">
    <div class="col-md-3 col-md-offset-9">
        <a class="btn btn-primary pull-right" href="@Url.Action("Create", "BitacoraInstalacion")"><i class="fa fa-fw fa-plus"></i> Agregar</a>
    </div>
</div>
<br />
<div class="row">
    @if (ViewBag.SuccessMessage != null)
    {
        <div class="col-md-4 col-md-offset-4">
            <div class="alert alert-success alert-dismissable">
                <button class="close" aria-hidden="true" type="button" data-dismiss="alert">×</button>
                @ViewBag.SuccessMessage
            </div>
        </div>
    }
    <div class="col-md-12">
        @if (Model.Count() > 0)
        {
            <table class="table table-striped table-bordered table-hover table-condensed data">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.id1)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.fecha1)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.detalles1)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.proc_instalacion1)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.id1)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.fecha1)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.detalles1)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.proc_instalacion1)
                            </td>

                            <td>
                                <a href="@Url.Action("Edit", "BitacoraInstalacion", new { id = item.id1 })" title="" class="btn btn-primary btn-xs action"
                                   data-original-title="Editar" data-toggle="tooltip" data-placement="top">
                                    <i class="fa fa-fw fa-edit"></i>
                                </a>
                                @*
                                    @Html.ActionLink("Details", "Details", new { id = item.id }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.id })
                                *@
                            </td>
                        </tr>
                    }
                </tbody>

            </table>
        }
        else
        {
            <h3>No hay datos que mostrar</h3>
        }
    </div>
</div>
@section customScripts{
    @Scripts.Render("~/bundles/dataTableScripts")
    <script type="text/javascript">
        $(document).ready(function () {
           $(".data").DataTable({
                responsive: true
            });

            $(".action").tooltip({
                container: "body"
            })
        });
    </script>
}
*/



