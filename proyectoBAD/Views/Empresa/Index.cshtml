@model IEnumerable<proyectoBAD.ViewModels.EmpresaViewModel>

@{
    ViewBag.Title = "Admon. Empresas";
    ViewBag.Pageheader = "Empresas";
}

@section customStyles{ 
    @Styles.Render("~/bundles/dataTableStyles")
}
<div class="row">
    <div class="col-md-3 col-md-offset-9">
        <a class="btn btn-primary pull-right" href="@Url.Action("Create", "Empresa")"><i class="fa fa-fw fa-plus"></i> Agregar</a>
    </div>
</div>
<br />
<div class="row">
    
    @if (ViewBag.SuccessMessage != null)
    {
        <div class="col-md-4 col-md-offset-4">
            <div class="alert alert-success alert-dismissable">
                <button class="close" aria-hidden="true" type="button" data-dismiss="alert">×</button>
                @ViewBag.SuccessMessage
            </div>
        </div>
    }
    <div class="col-md-12">
        @if (Model.Count() > 0)
        {
            <table class="table table-striped table-bordered table-hover table-condensed data">
                <thead>
                    <tr role="row">
                        <th>
                            @Html.DisplayNameFor(model => model.IDEMP)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.NOMBREEMP)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.TELEFONOCONTACTO)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.EMAILCONTACTO)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.NOMBREEMP)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.TIPOEMP)
                        </th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr role="row">
                            <td>
                                @Html.DisplayFor(modelItem => item.IDEMP)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NOMBREEMP)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TELEFONOCONTACTO)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.EMAILCONTACTO)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NOMBRERESPEMP)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TIPOEMP.tipo)
                            </td>
                            <td>
                                <a href="@Url.Action("Edit", "Empresa", new { id = item.IDEMP })" title=""  class="btn btn-primary btn-xs action"
                                   data-original-title="Editar" data-toggle="tooltip" data-placement="top">
                                    <i class="fa fa-fw fa-edit"></i>
                                </a>
                                @*
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                                *@
                            </td>
                        </tr>
                    }
                </tbody>
        </table>
            }
            else
            {
                <h3>No hay datos que mostrar</h3>
            }
    </div>
</div>
@section customScripts{
    @Scripts.Render("~/bundles/dataTableScripts")
    <script type="text/javascript">
        $(document).ready(function () {
           $(".data").DataTable({
                responsive: true
            });

            $(".action").tooltip({
                container: "body"
            })
        });
    </script>
}
